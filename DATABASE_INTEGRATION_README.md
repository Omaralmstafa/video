# تطبيق مشاركة الفيديوهات - Reels 📹

تطبيق Django لمشاركة ومشاهدة الفيديوهات مع قاعدة بيانات كاملة.

## ✨ المميزات الرئيسية

- 🎬 **مشغل ريلز** - عرض الفيديوهات في نمط ريلز (Tiktok/Instagram style)
- 🗄️ **قاعدة بيانات** - تخزين كل الفيديوهات وبيانات المستخدمين
- 📊 **إحصائيات** - تتبع عدد المشاهدات والإعجابات
- 🔍 **البحث والفرز** - البحث عن الفيديوهات بسهولة
- 📱 **تجاوب** - واجهة مستجيبة للهواتف والأجهزة اللوحية
- 🎨 **Tailwind CSS** - تصميم حديث وأنيق
- ⚡ **أداء عالي** - دعم streaming والـ range requests

## 🗄️ الهيكل

```
video_project/
├── video_project/           # إعدادات المشروع
│   ├── settings.py         # إعدادات Django
│   ├── urls.py             # الروابط الرئيسية
│   └── wsgi.py             # WSGI config
├── video_share/            # التطبيق الرئيسي
│   ├── models.py           # نموذج Video في قاعدة البيانات ✓
│   ├── views.py            # المنطق (Views) ✓
│   ├── urls.py             # الروابط ✓
│   ├── admin.py            # لوحة التحكم ✓
│   └── templates/
│       ├── video_list.html      # قائمة الفيديوهات ✓
│       ├── video_player.html    # مشغل الريلز ✓
│       ├── video_detail.html    # تفاصيل الفيديو ✓
│       └── upload.html          # رفع فيديو
├── media/                  # الفيديوهات المرفوعة
│   └── videos/
├── staticfiles/            # الملفات الثابتة
│   └── css/
│       └── tailwind.css
├── manage.py
├── requirements.txt        # المكتبات المطلوبة
└── import_videos.py        # سكريبت استيراد الفيديوهات ✓
```

## 🚀 البدء السريع

### 1. التثبيت

```bash
# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 2. إعداد قاعدة البيانات

```bash
# إنشاء الجداول
python manage.py migrate

# إنشاء مستخدم Admin
python manage.py createsuperuser

# (اختياري) استيراد الفيديوهات الموجودة من مجلد media/videos
python manage.py runserver  # في terminal آخر
python import_videos.py
```

### 3. بناء CSS (Tailwind)

```bash
# تثبيت المتطلبات
npm install

# بناء CSS
npm run build:css

# أو المراقبة المباشرة
npm run dev:css
```

### 4. تجميع الملفات الثابتة

```bash
python manage.py collectstatic --noinput
```

### 5. تشغيل الخادم

```bash
python manage.py runserver
```

ثم افتح: http://localhost:8000

## 📦 نموذج البيانات (Video Model)

```python
class Video(models.Model):
    title           - CharField(200)      # عنوان الفيديو
    description     - TextField           # الوصف
    file            - FileField           # ملف الفيديو
    thumbnail       - ImageField          # صورة مصغرة
    uploaded_at     - DateTimeField       # وقت التحميل
    updated_at      - DateTimeField       # وقت التحديث
    views           - IntegerField        # عدد المشاهدات
    likes           - IntegerField        # عدد الإعجابات
    is_published    - BooleanField        # هل الفيديو منشور
```

## 🔗 الروابط الرئيسية

| الرابط | الوصف |
|--------|-------|
| `/` | الصفحة الرئيسية - قائمة الفيديوهات |
| `/reels/` | مشغل الريلز (أول فيديو) |
| `/reels/<id>/` | مشغل الريلز (فيديو محدد) |
| `/video/<id>/` | صفحة تفاصيل الفيديو |
| `/stream/<id>/` | بث الفيديو (streaming) |
| `/upload/` | صفحة رفع فيديو جديد |
| `/api/like/<id>/` | API للإعجاب بفيديو |
| `/admin/` | لوحة التحكم |

## 👨‍💼 إدارة الفيديوهات

### عبر لوحة التحكم Admin

1. اذهب إلى: http://localhost:8000/admin/
2. ادخل ببيانات المسؤول
3. اضغط على "Videos"
4. أضف أو عدّل الفيديوهات

### عبر السكريبت

```bash
python import_videos.py
```

هذا السكريبت يقرأ الفيديوهات من `media/videos/` ويضيفها إلى قاعدة البيانات.

## 🎮 الميزات التفاعلية

### مشغل الريلز
- ⬆️ اسحب لأعلى - الفيديو التالي
- ⬇️ اسحب لأسفل - الفيديو السابق
- 🖱️ اضغط على الفيديو - تشغيل/إيقاف
- ⌨️ أسهم لوحة المفاتيح - التنقل
- Space - تشغيل/إيقاف

### الأزرار
- ❤️ الإعجاب - إضافة إعجاب
- ➤ مشاركة - مشاركة على وسائل التواصل
- ⬇️ تحميل - تحميل الفيديو
- ⋮ القائمة - خيارات إضافية

## 📊 الإحصائيات

كل فيديو يتتبع:
- عدد المشاهدات (يزداد عند كل بث)
- عدد الإعجابات (يتحدث عند الضغط على زر الإعجاب)
- وقت التحميل والتحديث

## 🐛 استكشاف الأخطاء

### الفيديوهات لا تظهر
```bash
# تحقق من قاعدة البيانات
python manage.py shell
>>> from video_share.models import Video
>>> Video.objects.all()

# تحقق من الملفات
ls media/videos/
```

### CSS لا يحمّل
```bash
# أعد بناء وتجميع
npm run build:css
python manage.py collectstatic --noinput
```

### خطأ في الصفحة
```bash
# تحقق من السجلات
python manage.py runserver  # شاهد الأخطاء

# أعد إنشاء الهجرات
python manage.py makemigrations
python manage.py migrate
```

## 📝 ملاحظات مهمة

1. **الفيديوهات الكبيرة**: يدعم streaming و range requests للفيديوهات الكبيرة
2. **الأمان**: استخدم `python manage.py check --deploy` قبل النشر
3. **الأداء**: استخدم Gunicorn بدلاً من development server في الإنتاج
4. **CDN**: استخدم CDN لخدمة الفيديوهات والملفات الثابتة

## 🔒 الأمان

- [x] CSRF protection معفل
- [x] SQL injection protection
- [x] CORS headers صحيحة
- [x] Range requests محمية

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم البناء بـ Django ❤️ و Tailwind CSS 🎨**
