# 📤 تعليمات رفع المشروع إلى GitHub

## الطريقة السريعة (Windows)

### 1. شغّل الملف التلقائي
```bash
رفع_إلى_GitHub.bat
```

### 2. أو نفّذ الأوامر يدوياً:

```bash
# 1. إضافة جميع الملفات
git add .

# 2. إنشاء Commit
git commit -m "Initial commit: Video sharing project with Django"

# 3. إضافة Remote (إذا لم يكن موجوداً)
git remote add origin https://github.com/Omaralmstafa/video.git

# 4. رفع المشروع
git push -u origin main
```

---

## الطريقة اليدوية (خطوة بخطوة)

### الخطوة 1: التحقق من Git
```bash
git --version
```

### الخطوة 2: تهيئة Git (إذا لم يكن مهيأ)
```bash
git init
```

### الخطوة 3: إضافة الملفات
```bash
git add .
```

### الخطوة 4: إنشاء Commit
```bash
git commit -m "Initial commit: Video sharing project with Django - Ready for Render deployment"
```

### الخطوة 5: إضافة Remote Repository
```bash
git remote add origin https://github.com/Omaralmstafa/video.git
```

**ملاحظة**: إذا كان Remote موجوداً بالفعل:
```bash
git remote set-url origin https://github.com/Omaralmstafa/video.git
```

### الخطوة 6: تغيير Branch إلى main
```bash
git branch -M main
```

### الخطوة 7: رفع المشروع
```bash
git push -u origin main
```

---

## 🔐 إعداد Personal Access Token

GitHub لم يعد يقبل كلمات المرور العادية. استخدم Token:

### 1. إنشاء Token
1. اذهب إلى: https://github.com/settings/tokens
2. اضغط "Generate new token (classic)"
3. اختر الصلاحيات:
   - ✅ `repo` (Full control of private repositories)
4. اضغط "Generate token"
5. **انسخ الـ Token فوراً** (لن يظهر مرة أخرى!)

### 2. استخدام Token
عند الرفع:
- **Username**: `Omaralmstafa`
- **Password**: الصق الـ Token (ليس كلمة المرور!)

---

## ✅ التحقق من النجاح

بعد الرفع، افتح:
https://github.com/Omaralmstafa/video

يجب أن ترى جميع الملفات!

---

## 🔄 تحديثات مستقبلية

عند إجراء تغييرات:

```bash
git add .
git commit -m "وصف التغييرات"
git push
```

---

## ⚠️ ملاحظات مهمة

### الملفات التي لن تُرفع (بفضل .gitignore):
- ✅ `db.sqlite3` - قاعدة البيانات المحلية
- ✅ `venv/` - البيئة الافتراضية
- ✅ `__pycache__/` - ملفات Python المؤقتة
- ✅ `media/` - الملفات المرفوعة (لن تُرفع)
- ✅ `staticfiles/` - الملفات الثابتة (سيتم إنشاؤها في Render)

### الملفات المهمة التي ستُرفع:
- ✅ `requirements.txt`
- ✅ `Procfile`
- ✅ `render.yaml`
- ✅ `settings.py`
- ✅ جميع ملفات الكود
- ✅ القوالب (templates)
- ✅ ملفات الشرح

---

## 🆘 حل المشاكل

### مشكلة: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/Omaralmstafa/video.git
```

### مشكلة: "Authentication failed"
- استخدم Personal Access Token بدلاً من كلمة المرور
- تأكد من نسخ Token بشكل صحيح

### مشكلة: "Repository not found"
- تأكد من أن المستودع موجود على GitHub
- تحقق من اسم المستخدم: `Omaralmstafa`
- تحقق من اسم المستودع: `video`

### مشكلة: "Large files"
```bash
# تحقق من .gitignore
cat .gitignore

# تأكد من أن الملفات الكبيرة في .gitignore
```

---

## 📋 قائمة التحقق

قبل الرفع، تأكد من:
- [ ] جميع التغييرات محفوظة
- [ ] `.gitignore` محدث
- [ ] `README.md` موجود
- [ ] `requirements.txt` محدث
- [ ] `Procfile` موجود
- [ ] Personal Access Token جاهز

---

## 🎉 بعد الرفع

1. ✅ افتح المستودع على GitHub
2. ✅ أضف وصف: "Django video sharing platform"
3. ✅ أضف Topics: `django`, `python`, `video-sharing`
4. ✅ أضف License: MIT
5. ✅ اربط المستودع بـ Render للنشر!

---

**جاهز للرفع؟** شغّل `رفع_إلى_GitHub.bat` أو اتبع الخطوات اليدوية أعلاه! 🚀

